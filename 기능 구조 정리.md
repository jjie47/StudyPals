# 프로젝트 구조

StudyPals/
├── main.py
├── firebase.py
├── auth.py
├── hud.py
├── tray.py
├── group.py
├── activity.py
├── settings.py
├── assets/
│   ├── animals/
│   │   ├── cat_active.gif
│   │   ├── cat_idle.png
│   │   ├── dog_active.gif
│   │   └── dog_idle.png
│   └── tray_icon.png
└── config.json


# 파일별 기능 정리
[main.py] — 앱 진입점
+ 앱 초기화
+ config.json 읽어서 자동로그인 토큰 확인
+ 자동로그인 성공 시 HUD 실행, 실패 시 로그인창 실행
+ 각 모듈 연결 및 실행 순서 조율


[firebase.py] — Firebase 연동
+ Firebase 앱 초기화 (인증 정보 로드)
+ Firestore DB 인스턴스 반환
+ Firebase Auth 인스턴스 반환
+ 실시간 리스너 연결/해제 유틸리티 함수


[auth.py] — 로그인 / 회원가입
+ 회원가입
    + ID 중복 확인 (Firestore 조회)
    + 비밀번호 암호화 (bcrypt 또는 Firebase Auth 위임)
    + Firestore에 유저 문서 생성 (ID, PW해시, Nickname, animal)
+ 로그인
    + ID 조회 → 비밀번호 대조
    + 로그인 성공 시 세션 토큰 발급 → config.json 저장 (자동로그인용)
+ 자동로그인
    + config.json의 토큰 유효성 Firebase에 검증
+ 로그아웃
    + 토큰 삭제, config.json 초기화


[activity.py] — 활동 감지
+ 키보드/마우스 이벤트 감지 (pynput 라이브러리)
    + 이벤트 감지 시 → 상태를 active로 변경
    + 마지막 입력 시각 기록
+ idle 판단 타이머
    + 마지막 입력으로부터 10초 경과 시 → 상태를 idle로 변경
+ Firebase 상태 업데이트
    + 상태 변경 시 Firestore 내 본인 유저 문서의 status 필드 갱신


[hud.py] — HUD 창 (메인화면)
+ 창 속성 설정
    + 항상 최상단 유지 (topmost)
    + 배경 투명 처리
    + 모니터 우측 하단 초기 위치 설정
    + 테두리 없는 창 (overrideredirect)
+ 드래그 이동
    + 마우스 클릭/드래그 이벤트로 창 위치 이동
+ 동물 카드 렌더링
    + 내 동물 카드 (제일 왼쪽 고정)
    + 그룹원 동물 카드 (오른쪽으로 나열)
    + active 상태 → GIF 재생 / idle 상태 → PNG 표시
    + 카드 아래 Nickname 표시
+ 실시간 상태 수신
    + Firestore 리스너로 그룹원 상태 변경 감지 → 즉시 GIF/PNG 전환
+ 우클릭 이벤트
    + 우클릭 시 설정 메뉴(settings.py) 호출


[tray.py] — 시스템 트레이
+ 트레이 아이콘 등록 (pystray 라이브러리)
+ 좌클릭 → HUD 창 보이기/숨기기 토글
+ 우클릭 → 설정 메뉴 표시
+ 앱 종료 시 트레이 아이콘 제거


[group.py] — 그룹 관리
+ 그룹 생성
    + 그룹 이름 입력 → 랜덤 그룹코드 생성 → Firestore에 그룹 문서 생성
    + 생성한 유저를 그룹 멤버로 자동 추가
+ 그룹 입장
    + 그룹코드로 Firestore 조회 → 존재하면 해당 유저를 멤버로 추가
+ 그룹 퇴장
    + 해당 그룹 문서에서 본인 멤버 제거
    + 멤버가 0명이 되면 그룹 문서 삭제
+ 그룹 목록 조회
    + 본인이 포함된 그룹 전체 조회
+ 그룹원 상태 조회
    + 특정 그룹의 멤버 목록 가져오기 → HUD에 전달


[settings.py] — 설정 메뉴 / 설정 창들
+ 설정 메뉴 팝업
    + Nickname 변경 / 동물 변경 / 그룹 관리 / 로그아웃 / 종료 항목
+ Nickname 변경 창
    + 변경할 닉네임 입력 → 중복 확인 → 저장 시 Firestore 업데이트
+ 동물 변경 창
    + 드롭박스로 동물 선택 → 저장 시 Firestore 업데이트 → HUD 즉시 반영
+ 그룹 관리 창
    + 내 그룹 목록 나열 + 그룹코드 표시 + 퇴장 버튼
    + 그룹코드 입력 + 입장 버튼
    + 새 그룹 생성 버튼


# 권장 작업 순서
1단계 (기반)     firebase.py → auth.py → config.json 구조
2단계 (감지)     activity.py (로컬 감지 먼저, Firebase 연동은 나중)
3단계 (화면)     hud.py (정적 UI 먼저, GIF/PNG 전환 구현)
4단계 (트레이)   tray.py
5단계 (그룹)     group.py
6단계 (설정)     settings.py
7단계 (통합)     main.py에서 전체 연결 + 실시간 리스너 붙이기